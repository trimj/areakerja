<header class="border-0 relative">
    <div class="header">
        <div class="flex items-center justify-between py-5">
            <div class="w-14">
                <a href="{{ route('public.home') }}">
                    <img src="{{ asset('assets/public/images/logo.png') }}" alt="{{ 'Logo' . config('app.name', 'Area Kerja') }}">
                </a>
            </div>
        </div>
    </div>
</header>
